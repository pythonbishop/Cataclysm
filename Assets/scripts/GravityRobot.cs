using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GravityRobot : MonoBehaviour
{
    // Start is called before the first frame update
    public float gravityForce;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
